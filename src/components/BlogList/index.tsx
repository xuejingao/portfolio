import React from "react";

import styles from "./style.module.css";

const BlogList: React.FC = () => {
  return (
    <div className={styles.container}>The blog page is under contruction!</div>
  );
};

export default BlogList;
